{
  "name": "spicy-discount-searcher",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Chrome/Brave extension that races corporate discount codes against each other to find the cheapest rental car or hotel rate.",
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "scripts": {
    "build": "npm run build:main && npm run build:content && npm run build:reset",
    "build:main": "tsc --noEmit && vite build",
    "build:content": "vite build --config vite.content.config.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "verify": "npm run typecheck && npm run lint && npm run format:check && npm run test && npm run build && node scripts/check-dist.mjs && npm audit --audit-level=moderate",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "codes": "./scripts/extract_codes.py",
    "icons": "./scripts/make-icons.sh",
    "build:reset": "vite build --config vite.reset.config.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/chrome": "^0.2.2",
    "@types/node": "^22.7.0",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "jsdom": "^30.0.1",
    "prettier": "^3.3.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
